import { style } from '@vanilla-extract/css';

import * as shared from './shared.css';

export const avatar = style([shared.avatar]);

export const item = style([shared.recentItem]);

export const row = style([shared.recentRow]);

export const text = style([shared.text]);
