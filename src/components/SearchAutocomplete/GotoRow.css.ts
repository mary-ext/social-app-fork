import { style } from '@vanilla-extract/css';

import * as shared from './shared.css';

export const icon = style([shared.icon]);

export const label = style([shared.label]);

export const row = style([shared.row]);
