/**
 * Max rendered height (px) for a standalone post image or video. The media keeps its own aspect ratio and
 * scales down to fit this height, so a tall portrait sits narrow rather than dominating the column.
 */
export const MAX_MEDIA_HEIGHT = 430;
