import * as styles from './DividerRow.css';

/** the thin separator between the suggestion list and its operator options. */
export function DividerRow() {
	return <div className={styles.divider} />;
}
