/** card width in pixels. */
export const DESIGN_WIDTH = 1280;

/** card height in pixels. */
export const DESIGN_HEIGHT = 720;

/** output frames per second. */
export const FRAME_RATE = 30;

/** avatar radius in pixels. */
export const AVATAR_RADIUS = 180;

/** halo cycle duration, in seconds. */
export const PULSE_PERIOD = 0.5;

/** per-circle opacity; overlaps compound. */
export const LOBE_OPACITY = 0.2;

/** peak halo displacement as a fraction of the avatar radius. */
export const LOBE_DISPLACEMENT = 0.36;

/** horizontal inset in pixels. */
export const MARGIN = 42;

/** text baseline in pixels from the top. */
export const TEXT_BASELINE = 671.5;

/** font size in pixels, giving Inter a cap height of about 30 px. */
export const FONT_SIZE = 42;

/** text and icon opacity. */
export const INK_OPACITY = 0.55;

/** speaker icon size in pixels. */
export const ICON_SIZE = 42;

/** speaker icon top edge in pixels. */
export const ICON_TOP = 635;

/** gap in pixels from the duration's advance width to the icon's ink. */
export const ICON_GAP = 20;
