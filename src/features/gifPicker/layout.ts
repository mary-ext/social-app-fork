import { space } from '#/styles/tokens.css';

export const DIALOG_PADDING = space.lg;

export const SEARCH_HEIGHT = 42;

/** strip below the search field where scrolled content fades out. */
export const SEARCH_FADE = space.sm;
