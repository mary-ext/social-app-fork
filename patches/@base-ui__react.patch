diff --git a/autocomplete/root/AutocompleteRoot.d.mts b/autocomplete/root/AutocompleteRoot.d.mts
index 2f18bd5..d62c323 100644
--- a/autocomplete/root/AutocompleteRoot.d.mts
+++ b/autocomplete/root/AutocompleteRoot.d.mts
@@ -25,6 +25,8 @@ export declare function AutocompleteRoot<ItemValue>(props: Omit<AutocompleteRoot
 export interface AutocompleteRootState extends AriaComboboxState {}
 export interface AutocompleteRootActions {
   unmount: () => void;
+  /** Highlights the list item at the given index (or clears it with `null`). */
+  setActiveIndex: (index: number | null) => void;
 }
 export type AutocompleteRootChangeEventReason = AriaCombobox.ChangeEventReason;
 export type AutocompleteRootChangeEventDetails = AriaCombobox.ChangeEventDetails;
diff --git a/combobox/root/AriaCombobox.d.mts b/combobox/root/AriaCombobox.d.mts
index 1f1cde9..ac34e46 100644
--- a/combobox/root/AriaCombobox.d.mts
+++ b/combobox/root/AriaCombobox.d.mts
@@ -107,6 +107,13 @@ interface ComboboxRootProps<ItemValue> {
    * Call this after any externally controlled closing animation finishes.
    */
   actionsRef?: React.RefObject<AriaCombobox.Actions | null> | undefined;
+  /**
+   * Only meaningful alongside `actionsRef`, which otherwise opts into manual unmounting (call
+   * `actions.unmount()` after your closing animation finishes). Set this to `true` to keep the popup
+   * unmounting automatically once its closing transition finishes while still using `actionsRef`.
+   * Without `actionsRef` the popup always unmounts automatically and this has no effect.
+   */
+  autoUnmount?: boolean | undefined;
   /**
    * Callback fired when an item is highlighted or unhighlighted.
    * Receives the highlighted item value (or `undefined` if no item is highlighted) and event details with a `reason` property describing why the highlight changed.
diff --git a/combobox/root/AriaCombobox.mjs b/combobox/root/AriaCombobox.mjs
index 3864151..ceb26ff 100644
--- a/combobox/root/AriaCombobox.mjs
+++ b/combobox/root/AriaCombobox.mjs
@@ -32,6 +32,7 @@ import { isScrollableY } from "../../utils/scrollable.mjs";
 import { useValueChanged } from "../../internals/useValueChanged.mjs";
 import { NOOP } from "../../internals/noop.mjs";
 import { FOCUSABLE_POPUP_PROPS } from "../../utils/popups/index.mjs";
+import { useRegisterPopupCloseAction } from "../../utils/popups/closeAllPopups.mjs";
 import { mergeProps } from "../../merge-props/index.mjs";
 import { stringifyAsLabel, stringifyAsValue, isGroupedItems } from "../../internals/resolveValueLabel.mjs";
 import { compareItemEquality, defaultItemEquality, findSelectionIndex, removeItem, selectedValueIncludes } from "../../internals/itemEquality.mjs";
@@ -644,7 +645,13 @@ export function AriaCombobox(props) {
       }
     }
   });
-
+  const closeImmediately = useStableCallback(() => {
+    if (open) {
+      setOpen(false, createChangeEventDetails(REASONS.imperativeAction));
+    }
+    handleUnmount();
+  });
+  useRegisterPopupCloseAction(mounted && !inline, closeImmediately);
   // Support composing the Dialog component around an inline combobox.
   // `[role="dialog"]` is more interoperable than using a context, e.g. it can work
   // with third-party modal libraries, though the limitation is that the closest
@@ -658,7 +665,7 @@ export function AriaCombobox(props) {
     return popupRef;
   }, [inline, positionerElement]);
   useOpenChangeComplete({
-    enabled: !props.actionsRef,
+    enabled: !props.actionsRef || props.autoUnmount === true,
     open,
     ref: resolvedPopupRef,
     onComplete() {
@@ -668,8 +675,12 @@ export function AriaCombobox(props) {
     }
   });
   React.useImperativeHandle(props.actionsRef, () => ({
-    unmount: handleUnmount
-  }), [handleUnmount]);
+    unmount: handleUnmount,
+    setActiveIndex: index => setIndices({
+      activeIndex: index,
+      type: 'none'
+    })
+  }), [handleUnmount, setIndices]);
   useIsoLayoutEffect(function syncClosedState() {
     if (open) {
       return;
@@ -1139,4 +1150,4 @@ export function AriaCombobox(props) {
       })
     })
   });
-}
\ No newline at end of file
+}
diff --git a/dialog/root/useRenderDialogRoot.mjs b/dialog/root/useRenderDialogRoot.mjs
index 49111df..177ff3a 100644
--- a/dialog/root/useRenderDialogRoot.mjs
+++ b/dialog/root/useRenderDialogRoot.mjs
@@ -1,6 +1,9 @@
 'use client';
 
 import * as React from 'react';
+import { addEventListener } from '@base-ui/utils/addEventListener';
+import { ownerWindow } from '@base-ui/utils/owner';
+import { platform } from '@base-ui/utils/platform';
 import { DialogInteractions } from "./useDialogRoot.mjs";
 import { DialogRootContext, useDialogRootContext } from "./DialogRootContext.mjs";
 import { DialogStore } from "../store/DialogStore.mjs";
@@ -55,8 +58,33 @@ export function useRenderDialogRoot(mode, props) {
   const open = store.useState('open');
   const mounted = store.useState('mounted');
   const payload = store.useState('payload');
+  const popupElement = store.useState('popupElement');
+  const nestedOpenDialogCount = store.useState('nestedOpenDialogCount');
+  const isTopmost = nestedOpenDialogCount === 0;
   usePopupRootSync(store, open);
   useImplicitActiveTrigger(store);
+  React.useEffect(() => {
+    if (!open || !isTopmost || !platform.os.android) {
+      return undefined;
+    }
+    const win = ownerWindow(popupElement);
+    const CloseWatcherCtor = win.CloseWatcher;
+    if (!CloseWatcherCtor) {
+      return undefined;
+    }
+    function handleCloseWatcher(event) {
+      if (!store.select('open')) {
+        return;
+      }
+      store.setOpen(false, createChangeEventDetails(REASONS.closeWatcher, event));
+    }
+    const closeWatcher = new CloseWatcherCtor();
+    const unsubscribe = addEventListener(closeWatcher, 'close', handleCloseWatcher);
+    return () => {
+      unsubscribe();
+      closeWatcher.destroy();
+    };
+  }, [store, isTopmost, open, popupElement]);
   const {
     forceUnmount
   } = useOpenStateTransitions(open, store);
diff --git a/drawer/root/DrawerRoot.mjs b/drawer/root/DrawerRoot.mjs
index 4ffbea9..dbe9b6c 100644
--- a/drawer/root/DrawerRoot.mjs
+++ b/drawer/root/DrawerRoot.mjs
@@ -2,12 +2,9 @@
 
 var _DrawerProviderReport, _DrawerProviderReport2;
 import * as React from 'react';
-import { addEventListener } from '@base-ui/utils/addEventListener';
 import { useControlled } from '@base-ui/utils/useControlled';
 import { useIsoLayoutEffect } from '@base-ui/utils/useIsoLayoutEffect';
 import { useStableCallback } from '@base-ui/utils/useStableCallback';
-import { ownerWindow } from '@base-ui/utils/owner';
-import { platform } from '@base-ui/utils/platform';
 import { DrawerRootContext, useDrawerRootContext } from "./DrawerRootContext.mjs";
 import { createChangeEventDetails } from "../../internals/createBaseUIEventDetails.mjs";
 import { REASONS } from "../../internals/reasons.mjs";
@@ -192,9 +189,6 @@ function DrawerProviderReporter() {
   const setDrawerOpen = providerContext?.setDrawerOpen;
   const removeDrawer = providerContext?.removeDrawer;
   const open = store.useState('open');
-  const nestedOpenDialogCount = store.useState('nestedOpenDialogCount');
-  const popupElement = store.useState('popupElement');
-  const isTopmost = nestedOpenDialogCount === 0;
   useIsoLayoutEffect(() => {
     if (!removeDrawer) {
       return undefined;
@@ -206,29 +200,5 @@ function DrawerProviderReporter() {
   useIsoLayoutEffect(() => {
     setDrawerOpen?.(store, open);
   }, [open, setDrawerOpen, store]);
-  React.useEffect(() => {
-    // CloseWatcher enables the Android back gesture (Chromium-only).
-    // Keep this Android-only for now to avoid interfering with Escape/nesting semantics on desktop due to `useDismiss`.
-    if (!open || !isTopmost || !platform.os.android) {
-      return undefined;
-    }
-    const win = ownerWindow(popupElement);
-    const CloseWatcherCtor = win.CloseWatcher;
-    if (!CloseWatcherCtor) {
-      return undefined;
-    }
-    function handleCloseWatcher(event) {
-      if (!store.select('open')) {
-        return;
-      }
-      store.setOpen(false, createChangeEventDetails(REASONS.closeWatcher, event));
-    }
-    const closeWatcher = new CloseWatcherCtor();
-    const unsubscribe = addEventListener(closeWatcher, 'close', handleCloseWatcher);
-    return () => {
-      unsubscribe();
-      closeWatcher.destroy();
-    };
-  }, [store, isTopmost, open, popupElement]);
   return null;
 }
\ No newline at end of file
diff --git a/menu/root/MenuRoot.mjs b/menu/root/MenuRoot.mjs
index 96dc92b..64aecda 100644
--- a/menu/root/MenuRoot.mjs
+++ b/menu/root/MenuRoot.mjs
@@ -130,6 +130,8 @@ export const MenuRoot = fastComponent(function MenuRoot(props) {
     forceUnmount
   } = useOpenStateTransitions(open, store, () => {
     store.set('allowMouseEnter', false);
+  }, {
+    registerForCloseAll: true
   });
   useIsoLayoutEffect(() => {
     if (contextMenuContext && !parentMenuRootContext) {
@@ -403,4 +405,4 @@ function useMenuRootStore(initialState) {
   // through an event).
   const store = useRefWithInit(() => new MenuStore(initialState)).current;
   return store;
-}
\ No newline at end of file
+}
diff --git a/package.json b/package.json
index ff5c0bf..fa8a1fe 100644
--- a/package.json
+++ b/package.json
@@ -662,6 +662,16 @@
         "default": "./types/index.mjs"
       }
     },
+    "./unstable-close-all-popups": {
+      "import": {
+        "types": "./unstable-close-all-popups/index.d.mts",
+        "default": "./unstable-close-all-popups/index.mjs"
+      },
+      "default": {
+        "types": "./unstable-close-all-popups/index.d.mts",
+        "default": "./unstable-close-all-popups/index.mjs"
+      }
+    },
     "./unstable-use-media-query": {
       "import": {
         "types": "./unstable-use-media-query/index.d.mts",
@@ -1275,4 +1285,4 @@
   },
   "main": "./index.js",
   "types": "./index.d.ts"
-}
\ No newline at end of file
+}
diff --git a/popover/root/PopoverRoot.mjs b/popover/root/PopoverRoot.mjs
index 35d6486..8c0fd1f 100644
--- a/popover/root/PopoverRoot.mjs
+++ b/popover/root/PopoverRoot.mjs
@@ -1,7 +1,10 @@
 'use client';
 
 import * as React from 'react';
-import { useDismiss, FloatingTree } from "../../floating-ui-react/index.mjs";
+import { addEventListener } from '@base-ui/utils/addEventListener';
+import { ownerWindow } from '@base-ui/utils/owner';
+import { platform } from '@base-ui/utils/platform';
+import { useDismiss, FloatingTree, useFloatingParentNodeId } from "../../floating-ui-react/index.mjs";
 import { PopoverRootContext, usePopoverRootContext } from "./PopoverRootContext.mjs";
 import { PopoverStore } from "../store/PopoverStore.mjs";
 import { createChangeEventDetails } from "../../internals/createBaseUIEventDetails.mjs";
@@ -45,6 +48,8 @@ function PopoverRootComponent({
       stickIfOpen: true,
       openChangeReason: null
     });
+  }, {
+    registerForCloseAll: true
   });
   store.useSyncedValues({
     modal
@@ -54,6 +59,30 @@ function PopoverRootComponent({
       store.context.stickIfOpenTimeout.clear();
     }
   }, [store, open]);
+  const nested = useFloatingParentNodeId() != null;
+  const popupElement = store.useState('popupElement');
+  React.useEffect(() => {
+    if (!open || nested || !platform.os.android) {
+      return undefined;
+    }
+    const win = ownerWindow(popupElement);
+    const CloseWatcherCtor = win.CloseWatcher;
+    if (!CloseWatcherCtor) {
+      return undefined;
+    }
+    function handleCloseWatcher(event) {
+      if (!store.select('open')) {
+        return;
+      }
+      store.setOpen(false, createChangeEventDetails(REASONS.closeWatcher, event));
+    }
+    const closeWatcher = new CloseWatcherCtor();
+    const unsubscribe = addEventListener(closeWatcher, 'close', handleCloseWatcher);
+    return () => {
+      unsubscribe();
+      closeWatcher.destroy();
+    };
+  }, [store, nested, open, popupElement]);
   React.useImperativeHandle(props.actionsRef, () => ({
     unmount: forceUnmount,
     close: () => store.setOpen(false, createChangeEventDetails(REASONS.imperativeAction))
@@ -128,4 +157,4 @@ function PopoverInteractions({
     popupProps
   });
   return null;
-}
\ No newline at end of file
+}
diff --git a/preview-card/root/PreviewCardRoot.mjs b/preview-card/root/PreviewCardRoot.mjs
index fac4f14..a89d3f7 100644
--- a/preview-card/root/PreviewCardRoot.mjs
+++ b/preview-card/root/PreviewCardRoot.mjs
@@ -43,6 +43,8 @@ function PreviewCardRootComponent(props) {
     forceUnmount
   } = useOpenStateTransitions(open, store, () => {
     store.context.inlineRectCoordsRef.current = undefined;
+  }, {
+    registerForCloseAll: true
   });
   useIsoLayoutEffect(() => {
     if (open) {
@@ -102,4 +104,4 @@ export const PreviewCardRoot = fastComponent(function PreviewCardRoot(props) {
     })
   });
 });
-if (process.env.NODE_ENV !== "production") PreviewCardRoot.displayName = "PreviewCardRoot";
\ No newline at end of file
+if (process.env.NODE_ENV !== "production") PreviewCardRoot.displayName = "PreviewCardRoot";
diff --git a/select/root/SelectRoot.mjs b/select/root/SelectRoot.mjs
index da1f59d..f2b42d0 100644
--- a/select/root/SelectRoot.mjs
+++ b/select/root/SelectRoot.mjs
@@ -31,8 +31,8 @@ import { useValueChanged } from "../../internals/useValueChanged.mjs";
 import { useOpenInteractionType } from "../../utils/useOpenInteractionType.mjs";
 import { getMaxScrollOffset, normalizeScrollOffset } from "../../utils/scrollEdges.mjs";
 import { FOCUSABLE_POPUP_PROPS } from "../../utils/popups/index.mjs";
+import { useRegisterPopupCloseAction } from "../../utils/popups/closeAllPopups.mjs";
 import { mergeProps } from "../../merge-props/index.mjs";
-
 /**
  * Groups all parts of the select.
  * Doesn't render its own HTML element.
@@ -235,6 +235,13 @@ export function SelectRoot(props) {
     });
     onOpenChangeComplete?.(false);
   });
+  const closeImmediately = useStableCallback(() => {
+    if (open) {
+      setOpen(false, createChangeEventDetails(REASONS.imperativeAction));
+    }
+    handleUnmount();
+  });
+  useRegisterPopupCloseAction(mounted, closeImmediately);
   useOpenChangeComplete({
     enabled: !actionsRef,
     open,
@@ -446,4 +453,4 @@ export function SelectRoot(props) {
       suppressHydrationWarning: true
     }), hiddenInputs]
   });
-}
\ No newline at end of file
+}
diff --git a/slider/control/SliderControl.mjs b/slider/control/SliderControl.mjs
index 6a2b200..52860b2 100644
--- a/slider/control/SliderControl.mjs
+++ b/slider/control/SliderControl.mjs
@@ -316,13 +316,15 @@ export const SliderControl = /*#__PURE__*/React.forwardRef(function SliderContro
       x: touch.clientX,
       y: touch.clientY
     };
-    startPressing(fingerCoords);
-    const finger = getFingerState(fingerCoords);
-    if (finger == null) {
-      return;
+    if (currentInteractionValueRef.current == null) {
+      startPressing(fingerCoords);
+      const finger = getFingerState(fingerCoords);
+      if (finger == null) {
+        return;
+      }
+      focusThumb(finger.thumbIndex);
+      setValueFromPointer(finger, REASONS.trackPress, nativeEvent);
     }
-    focusThumb(finger.thumbIndex);
-    setValueFromPointer(finger, REASONS.trackPress, nativeEvent);
     moveCountRef.current = 0;
     const doc = ownerDocument(controlRef.current);
     doc.addEventListener('touchmove', handleTouchMove, {
@@ -331,13 +333,18 @@ export const SliderControl = /*#__PURE__*/React.forwardRef(function SliderContro
     doc.addEventListener('touchend', handleTouchEnd, {
       passive: true
     });
+    doc.addEventListener('touchcancel', handleTouchEnd, {
+      passive: true
+    });
   });
   const stopListening = useStableCallback(() => {
     const doc = ownerDocument(controlRef.current);
     doc.removeEventListener('pointermove', handleTouchMove);
     doc.removeEventListener('pointerup', handleTouchEnd);
+    doc.removeEventListener('pointercancel', handleTouchEnd);
     doc.removeEventListener('touchmove', handleTouchMove);
     doc.removeEventListener('touchend', handleTouchEnd);
+    doc.removeEventListener('touchcancel', handleTouchEnd);
     pressedValuesRef.current = null;
     currentInteractionValueRef.current = null;
   });
@@ -411,6 +418,9 @@ export const SliderControl = /*#__PURE__*/React.forwardRef(function SliderContro
         doc.addEventListener('pointerup', handleTouchEnd, {
           once: true
         });
+        doc.addEventListener('pointercancel', handleTouchEnd, {
+          once: true
+        });
       }
     }, elementProps],
     stateAttributesMapping: sliderStateAttributesMapping
diff --git a/unstable-close-all-popups/index.d.mts b/unstable-close-all-popups/index.d.mts
new file mode 100644
index 0000000..bce2a13
--- /dev/null
+++ b/unstable-close-all-popups/index.d.mts
@@ -0,0 +1 @@
+export { closeAllPopups } from "../utils/popups/closeAllPopups.mjs";
diff --git a/unstable-close-all-popups/index.mjs b/unstable-close-all-popups/index.mjs
new file mode 100644
index 0000000..bce2a13
--- /dev/null
+++ b/unstable-close-all-popups/index.mjs
@@ -0,0 +1 @@
+export { closeAllPopups } from "../utils/popups/closeAllPopups.mjs";
diff --git a/utils/InternalBackdrop.mjs b/utils/InternalBackdrop.mjs
index 574e6e4..0994b9e 100644
--- a/utils/InternalBackdrop.mjs
+++ b/utils/InternalBackdrop.mjs
@@ -10,10 +10,6 @@ export const InternalBackdrop = /*#__PURE__*/React.forwardRef(function InternalB
     ...otherProps
   } = props;
   let clipPath;
-  if (cutout) {
-    const rect = cutout.getBoundingClientRect();
-    clipPath = `polygon(0% 0%,100% 0%,100% 100%,0% 100%,0% 0%,${rect.left}px ${rect.top}px,${rect.left}px ${rect.bottom}px,${rect.right}px ${rect.bottom}px,${rect.right}px ${rect.top}px,${rect.left}px ${rect.top}px)`;
-  }
   return /*#__PURE__*/_jsx("div", {
     ref: ref,
     role: "presentation"
diff --git a/utils/popups/closeAllPopups.d.mts b/utils/popups/closeAllPopups.d.mts
new file mode 100644
index 0000000..426254b
--- /dev/null
+++ b/utils/popups/closeAllPopups.d.mts
@@ -0,0 +1,4 @@
+/** closes every registered floating popup immediately, without waiting for an exit animation. */
+export declare function closeAllPopups(): void;
+/** @internal */
+export declare function useRegisterPopupCloseAction(mounted: boolean, close: () => void): void;
diff --git a/utils/popups/closeAllPopups.mjs b/utils/popups/closeAllPopups.mjs
new file mode 100644
index 0000000..90969e6
--- /dev/null
+++ b/utils/popups/closeAllPopups.mjs
@@ -0,0 +1,35 @@
+'use client';
+
+import { useIsoLayoutEffect } from '@base-ui/utils/useIsoLayoutEffect';
+
+const closeActions = new Set();
+
+/**
+ * closes every registered floating popup immediately, without waiting for an exit animation.
+ *
+ * dialogs and tooltips do not register themselves. controlled popups must honor their `onOpenChange`
+ * callback to remain closed after the immediate unmount.
+ */
+export function closeAllPopups() {
+  for (const close of Array.from(closeActions)) {
+    close();
+  }
+}
+
+/**
+ * registers an immediate close action while a floating popup is mounted.
+ *
+ * @param mounted whether the popup is mounted
+ * @param close action that closes and unmounts the popup
+ */
+export function useRegisterPopupCloseAction(mounted, close) {
+  useIsoLayoutEffect(() => {
+    if (!mounted) {
+      return undefined;
+    }
+    closeActions.add(close);
+    return () => {
+      closeActions.delete(close);
+    };
+  }, [mounted, close]);
+}
diff --git a/utils/popups/popupStoreUtils.d.mts b/utils/popups/popupStoreUtils.d.mts
index e606c10..e04997d 100644
--- a/utils/popups/popupStoreUtils.d.mts
+++ b/utils/popups/popupStoreUtils.d.mts
@@ -133,7 +133,9 @@ export declare function useImplicitActiveTrigger<State extends PopupStoreState<u
  *
  * @returns A function to forcibly unmount the popup.
  */
-export declare function useOpenStateTransitions<State extends PopupStoreState<unknown>>(open: boolean, store: ReactStore<State, PopupStoreContext<never>, typeof popupStoreSelectors>, onUnmount?: () => void): {
+export declare function useOpenStateTransitions<State extends PopupStoreState<unknown>>(open: boolean, store: ReactStore<State, PopupStoreContext<never>, typeof popupStoreSelectors>, onUnmount?: () => void, options?: {
+    registerForCloseAll?: boolean;
+}): {
   forceUnmount: () => void;
   transitionStatus: import("../../internals/useTransitionStatus.mjs").TransitionStatus;
 };
@@ -141,4 +143,4 @@ export declare function usePopupInteractionProps<State extends PopupStoreState<u
 export declare function usePopupRootSync<State extends PopupStoreState<unknown> & {
   openMethod: InteractionType | null;
 }>(store: ReactStore<State, PopupStoreContext<never>, typeof popupStoreSelectors>, open: boolean): void;
-export {};
\ No newline at end of file
+export {};
diff --git a/utils/popups/popupStoreUtils.mjs b/utils/popups/popupStoreUtils.mjs
index b762f79..56fd6bb 100644
--- a/utils/popups/popupStoreUtils.mjs
+++ b/utils/popups/popupStoreUtils.mjs
@@ -14,6 +14,7 @@ import { useTransitionStatus } from "../../internals/useTransitionStatus.mjs";
 import { useOpenChangeComplete } from "../../internals/useOpenChangeComplete.mjs";
 import { createChangeEventDetails } from "../../internals/createBaseUIEventDetails.mjs";
 import { REASONS } from "../../internals/reasons.mjs";
+import { useRegisterPopupCloseAction } from "./closeAllPopups.mjs";
 export const FOCUSABLE_POPUP_PROPS = {
   tabIndex: -1,
   [FOCUSABLE_ATTRIBUTE]: ''
@@ -211,16 +212,6 @@ export function useTriggerDataForwarding(triggerId, triggerElementRef, store, st
       });
       return;
     }
-    if (activeTriggerId == null && open) {
-      // If a popup is already open, a detached trigger can mount before any active trigger
-      // has been established. Claim the first registered trigger so trigger-owned focus
-      // management and ARIA relationships work.
-      store.update({
-        activeTriggerId: triggerId,
-        activeTriggerElement: element,
-        ...stateUpdates
-      });
-    }
   });
 
   // Intentionally NOT stable. Its identity is derived from `baseRegisterTrigger`, which is keyed on
@@ -374,7 +365,10 @@ export function useImplicitActiveTrigger(store, options = {}) {
  *
  * @returns A function to forcibly unmount the popup.
  */
-export function useOpenStateTransitions(open, store, onUnmount) {
+export function useOpenStateTransitions(open, store, onUnmount, options = {}) {
+  const {
+    registerForCloseAll = false
+  } = options;
   const {
     mounted,
     setMounted,
@@ -400,6 +394,13 @@ export function useOpenStateTransitions(open, store, onUnmount) {
     onUnmount?.();
     store.context.onOpenChangeComplete?.(false);
   });
+  const closeImmediately = useStableCallback(() => {
+    if (store.select('open')) {
+      store.setOpen(false, createChangeEventDetails(REASONS.imperativeAction));
+    }
+    forceUnmount();
+  });
+  useRegisterPopupCloseAction(registerForCloseAll && mounted, closeImmediately);
   useOpenChangeComplete({
     enabled: mounted && !open && !syncedPreventUnmountingOnClose,
     open,
@@ -436,4 +437,4 @@ export function usePopupRootSync(store, open) {
       store.set('openMethod', null);
     }
   }, [store]);
-}
\ No newline at end of file
+}
diff --git a/utils/useAnchoredPopupScrollLock.mjs b/utils/useAnchoredPopupScrollLock.mjs
index 98ccc07..e9fbbee 100644
--- a/utils/useAnchoredPopupScrollLock.mjs
+++ b/utils/useAnchoredPopupScrollLock.mjs
@@ -27,5 +27,5 @@ export function useAnchoredPopupScrollLock(enabled, touchOpen, positionerElement
     const popupWidth = positionerElement.offsetWidth;
     setTouchOpenShouldLockScroll(viewportWidth > 0 && popupWidth > 0 && popupWidth >= viewportWidth - VIEWPORT_WIDTH_TOLERANCE_PX);
   }, [enabled, touchOpen, positionerElement]);
-  useScrollLock(enabled && (!touchOpen || touchOpenShouldLockScroll), referenceElement);
+  useScrollLock(enabled, referenceElement);
 }
\ No newline at end of file
